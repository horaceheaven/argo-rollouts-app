apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-blue
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "nginx-demo.labels" . | nindent 4 }}
    version: blue
data:
  index.html: |-
{{ include "nginx-demo.htmlContent" (dict "root" . "env" "blue") | indent 4 }}

