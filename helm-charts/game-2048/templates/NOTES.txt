Thank you for installing {{ .Chart.Name }}!

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }} -n {{ .Release.Namespace }}
  $ helm get all {{ .Release.Name }} -n {{ .Release.Namespace }}

1. Get the application URL by running these commands:
{{- if .Values.ingress.enabled }}
  kubectl get ingress {{ include "game-2048.fullname" . }} -n {{ .Release.Namespace }}
  
  Get the Load Balancer hostname:
  echo "Application URL: http://$(kubectl get ingress {{ include "game-2048.fullname" . }} -n {{ .Release.Namespace }} -o jsonpath='{.status.loadBalancer.ingress[0].hostname}')"
  
  NOTE: It may take a few minutes for the LoadBalancer to be available.
{{- else }}
  export POD_NAME=$(kubectl get pods -n {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "game-2048.name" . }},app.kubernetes.io/instance={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")
  kubectl port-forward $POD_NAME 8080:{{ .Values.service.targetPort }} -n {{ .Release.Namespace }}
  echo "Visit http://127.0.0.1:8080 to use your application"
{{- end }}

2. Check pod status:
  kubectl get pods -n {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "game-2048.name" . }}"

3. View application logs:
  kubectl logs -f -n {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "game-2048.name" . }}"

4. Check deployment rollout status:
  kubectl rollout status deployment/{{ include "game-2048.fullname" . }} -n {{ .Release.Namespace }}

